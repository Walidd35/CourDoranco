// Create a function add with two parameter which returns the sum of two numbers

// Create a function substract with two parameter which returns the difference of two numbers

// Create a function divide with two parameter which returns the quotient of two numbers

// Create a function multiply with two parameter which returns the product of two numbers

// Create a variable tax = 20.5

export function add(x,y){return x + y}

function substract(x,y){return x - y}

export default function multiply(x,y){return x * y} // we export a funtion by default

function divide(x,y){return x / y}

export const tax = 20.5