// function add(a, b) {return a + b}

// module.exports = {
//     add: add, 
// };
// the fist add is the object's key and the second add is the reference to the function add

export function add(a, b) {return a + b}