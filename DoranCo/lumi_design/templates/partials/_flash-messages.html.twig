{% for type, message in app.flashes %}
    <div class="alert alert-{{type}} alert-dismissible fade show w-50 mx-auto my-2 text-center" role="alert"">
    {{message|join('. ')}}
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
{% endfor %}